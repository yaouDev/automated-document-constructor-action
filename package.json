{
  "name": "automated-document-constructor-action",
  "version": "1.0.0",
  "description": "A GitHub Action to wrap and run the Automated Document Constructor.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@actions/exec": "^1.1.1"
  }
}
